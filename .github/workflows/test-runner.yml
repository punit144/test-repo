name: "Test Ephemeral Runners"

on:
  push:
    branches: [ "main" ]

jobs:
  ephemeral-runner-job:
    runs-on: 
      - ephemeral-runners
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Hello ephemeral runner
        run: echo "Hello from an ephemeral runner!"
